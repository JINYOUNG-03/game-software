# 게임 개발 기본 지침

이 문서는 이 저장소에서 작업하는 Codex 에이전트가 유지해야 할 게임 개발의 기본 방향이다. 사용자가 명시적으로 변경을 요청하기 전까지 아래 원칙을 기본값으로 삼는다.

## 프로젝트 목표

- C++와 OpenGL을 기반으로 한 실시간 렌더링 게임을 제작한다.
- 장르는 쿼터뷰(아이소메트릭에 가까운) 2.5D 오픈월드 RPG다.
- 진짜 3D 원근 게임이 아니라, 월드의 `x`, `y`, `z` 좌표와 높이·깊이 정렬을 이용해 2.5D 공간감을 표현한다.
- 현재 프로젝트는 OpenGL, FreeGLUT, GLEW를 이용해 사각형을 그리는 최소 렌더링 기반에서 출발한다.

## 기술 설계 원칙

- 게임 상태 갱신과 화면 그리기를 분리한다. 기본 프레임 흐름은 `Input → Update(deltaTime) → World/Chunk Streaming → Visible Entity Sort → Render`다.
- 이벤트 콜백에 게임 로직을 쌓지 않는다. 입력 상태는 수집하고, 게임 로직은 `Update`에서 처리한다.
- 월드 좌표와 화면 좌표를 분리한다. 게임 오브젝트는 월드 좌표 `(x, y, z)`를 가지며, 카메라 또는 투영 계층이 이를 화면 좌표로 변환한다.
- 쿼터뷰 투영의 초기 기준식은 다음을 사용한다. 세부 수치와 카메라 오프셋은 구현 단계에서 조정할 수 있다.

```cpp
screenX = (worldX - worldY) * tileWidth * 0.5f;
screenY = (worldX + worldY) * tileHeight * 0.5f - worldZ;
```

- 렌더링은 화면에 보이는 대상만 처리하고, 엔티티는 깊이값(기본적으로 `worldX + worldY`, 높이와 스프라이트 발 위치를 고려해 확장 가능)으로 정렬한다.
- 월드는 청크 단위(초기 권장값: 32×32 타일)로 나눈다. 플레이어 주변 청크만 로드·업데이트·렌더링한다.
- 게임 도메인과 렌더러를 분리한다. 장기적으로 `Game`, `World`, `Chunk`, `Entity`, `Camera`, `Renderer`의 책임을 구분한다.
- 지형의 높이와 이동 가능 여부는 렌더링 전용 데이터가 아니라 이동, 충돌, 시야 및 상호작용에서 공유하는 월드 데이터로 다룬다.

## 개발 우선순위

다음의 작은 세로 단면 순서로 확장한다.

1. 쿼터뷰 타일맵과 플레이어 추적 카메라
2. 델타 타임 기반 플레이어 이동과 충돌
3. 단일 청크의 NPC 및 상호작용
4. 전투 또는 퀘스트 한 가지의 완결된 루프
5. 청크 경계 이동, 스트리밍, 저장
6. 월드 규모와 콘텐츠 확장

## 세계관과 분위기

- 실제 역사를 그대로 재현하지 않는 조선 후기풍 판타지 왕조를 배경으로 한다. 가칭 국호는 `해동국`이다.
- 궁궐과 권력자보다, 시장·포구·골목·주막·약방·성문·마을처럼 시민의 일상이 느껴지는 장소와 인물을 중요하게 다룬다.
- 세계는 완전히 붕괴하지 않았다. 사람들은 역병과 불안 속에서도 장사하고, 가족을 돌보고, 공동체를 지키며 살아간다.
- 정체불명의 역병(가칭 `청혈병`)이 세계 전반에 퍼져 있다. 병은 사회적 공포, 봉쇄, 약재 독점, 차별, 종교적 갈등, 정치적 은폐를 낳는다.
- 역병의 근원은 왕실·관료·금단의 의식·초자연적 존재와 연결되어 있지만, 단순한 악당의 음모로 환원하지 않는다. 과거의 절박한 선택이 현재 세대에 대가로 돌아온 구조를 지향한다.

## 서사와 플레이어 원칙

- 플레이어는 절대적인 선인이 아니다. 세상을 구할 가능성이 있으나, 누굴 구하고 무엇을 희생할지를 결정하는 위험한 인물이다.
- 플레이어의 목적은 인간적이고 납득 가능해야 하지만, 해결 수단은 은폐·협박·배제·희생처럼 도덕적으로 불편할 수 있다.
- 퀘스트와 주요 선택을 단순한 선/악 이분법 또는 점수표로 만들지 않는다. 한 집단을 살리는 선택은 다른 집단의 손실·불신·혼란을 낳을 수 있다.
- NPC들은 플레이어를 일관된 영웅이나 악인으로만 평가하지 않는다. 같은 선택도 계층, 지역, 피해 경험에 따라 다르게 기억하고 말한다.
- 엔딩은 선악 점수보다 플레이어의 결정 뒤에 남은 세계의 형태로 분기한다. 예: 역병은 사라졌지만 통제가 강해진 왕국, 희생 끝에 자치가 남은 지역, 진실이 공개되어 혼란을 겪는 세계.

## 작업 방식

- 사용자가 특별히 요청하지 않는 한 빌드, 실행, 결과 화면 확인은 사용자가 직접 수행한다. Codex는 코드 분석과 변경을 우선하며, 필요한 경우에만 빌드·실행을 제안하거나 수행한다.
- 새로운 기능은 위 기술 구조와 세계관 원칙을 훼손하지 않는 범위에서 작고 검증 가능한 단위로 구현한다.
- 사용자가 이 문서의 방향을 바꾸도록 명시하지 않는 한, 이후 설계·구현 제안은 이 지침을 기본 전제로 한다.
